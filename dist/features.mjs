// Release switches. Changing a flag requires publishing a new site version.
// Music stays dormant, including saved selections, while this is false.
// Recordings are not bundled. Enabling music requires licensed assets and credits.
export const FEATURES = Object.freeze({ music: false });
